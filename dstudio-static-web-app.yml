name: Azure Static Web Apps CI/CD

pr:
  branches:
    include:
      - main
  paths:
    exclude:
      - 'public/**'
      - 'README.md'
      - 'LICENCE'
      - 'dstudio-static-web-app.yml'
      - 'dstudio-static-web-app-alpha.yml'
trigger:
  branches:
    include:
      - main
  paths:
    exclude:
      - 'public/**'
      - 'README.md'
      - 'LICENCE'
      - 'dstudio-static-web-app.yml'
      - 'dstudio-static-web-app-alpha.yml'

jobs:
  - job: build_and_deploy_job
    displayName: Build and Deploy Job
    condition: or(eq(variables['Build.Reason'], 'Manual'),or(eq(variables['Build.Reason'], 'PullRequest'),eq(variables['Build.Reason'], 'IndividualCI')))
    pool:
      vmImage: ubuntu-latest
    variables:
      - group: Azure-Static-Web-Apps-brave-glacier-0460d6c1e-variable-group
    steps:
      - checkout: self
        submodules: true
      - task: AzureStaticWebApp@0
        inputs:
          azure_static_web_apps_api_token: $(AZURE_STATIC_WEB_APPS_API_TOKEN_BRAVE_GLACIER_0460D6C1E)
          ###### Repository/Build Configurations - These values can be configured to match your app requirements. ######
          # For more information regarding Static Web App workflow configurations, please visit: https://aka.ms/swaworkflowconfig
          app_location: "/" # App source code path
          api_location: "" # Api source code path - optional
          output_location: "" # Built app content directory - optional
###### End of Repository/Build Configurations ######

